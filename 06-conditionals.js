const checking = 1000;
const wednesday = true;
if(checking > 500 || wednesday) {
    console.log('balanced book');
} else {
    console.log('unbalanced book');
}

// Conditional Operators: == , === , != , !== , > , < , >= , <=
// Logic Operators: && , ||